'use client'
import { useCallback, useEffect, useState } from 'react'
import { Activity, AlertTriangle, BrainCircuit, Circle, RefreshCw, WifiOff } from 'lucide-react'
import { ArrowLink, SectionLabel, Shell, Status } from '@/components/astraos'
import { fetchRuntime, runtimeConfig, runtimeWebSocket, type RuntimeConnection } from '@/lib/runtime'

const panels = [['CPU','cpu'],['MEMORY','memory'],['PROCESSES','processes'],['DISK','disk'],['NETWORK','network'],['THERMAL','thermal']]
export default function RuntimePage(){ const [state,setState]=useState<RuntimeConnection>('CONNECTING'); const [metrics,setMetrics]=useState<Record<string,unknown>|null>(null); const [attempt,setAttempt]=useState(0)
 const connect=useCallback(()=>{ let active=true; setState('CONNECTING'); const controller=new AbortController(); fetchRuntime('/metrics',controller.signal).then(data=>{if(active){setMetrics(data);setState('LIVE')}}).catch(()=>{if(active){setMetrics(null);setState('OFFLINE')}}); const socket=runtimeWebSocket('/ws/telemetry'); if(socket){socket.onopen=()=>active&&setState('LIVE');socket.onmessage=e=>{try{setMetrics(JSON.parse(e.data))}catch{}};socket.onerror=()=>active&&setState('DEGRADED');socket.onclose=()=>active&&setState('OFFLINE')} return()=>{active=false;controller.abort();socket?.close()}
 },[attempt]); useEffect(()=>connect(),[connect])
 return <Shell><section className="border-b hairline"><div className="mx-auto max-w-7xl px-5 pb-14 pt-16 lg:px-8 lg:pb-20 lg:pt-24"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><SectionLabel number="RUNTIME / 00">LIVE SYSTEM SURFACE</SectionLabel><h1 className="mt-6 text-5xl font-medium tracking-[-.055em] md:text-6xl">Runtime command center.</h1><p className="mt-6 max-w-xl text-base leading-7 text-muted-foreground">A direct view into the AstraOS service layer. Live telemetry is never simulated; unavailable fields remain unavailable.</p></div><div className="flex items-center gap-4"><Status label={state} tone={state==='LIVE'?'accent':'muted'} /><button onClick={()=>setAttempt(a=>a+1)} className="inline-flex items-center gap-2 border hairline px-3 py-2 mono text-[10px] tracking-[.1em] hover:border-accent" aria-label="Retry runtime connection"><RefreshCw className="size-3" /> RETRY</button></div></div></div></section><div className="mx-auto max-w-7xl px-5 py-10 lg:px-8 lg:py-16">{state==='OFFLINE' && <div className="mb-8 flex items-start gap-4 border border-dashed hairline bg-card p-5"><WifiOff className="mt-1 size-5 text-muted-foreground" /><div><h2 className="text-sm font-semibold">No AstraOS runtime is currently bound to this deployment.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">The public engineering surface remains available. Live telemetry requires an AstraOS runtime endpoint configured through <code className="mono text-xs text-accent">VITE_API_BASE_URL</code> or <code className="mono text-xs text-accent">NEXT_PUBLIC_API_BASE_URL</code>.</p></div></div>}{!runtimeConfig.apiBase && <div className="mb-8 border-l-2 border-accent bg-card p-5"><div className="mono text-[10px] text-accent">NO ENDPOINT CONFIGURED</div><p className="mt-2 text-sm text-muted-foreground">Connect a FastAPI service to load actual metrics. This surface intentionally does not render fallback telemetry.</p></div>}<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{panels.map(([label,key])=><div key={key} className="border hairline bg-card p-5"><div className="flex justify-between"><span className="mono text-[10px] text-muted-foreground">{label}</span><Activity className="size-4 text-muted-foreground" /></div><div className="mt-10 mono text-2xl text-foreground">{metrics?.[key] !== undefined ? String(metrics[key]) : '—'}</div><div className="mt-2 mono text-[10px] text-muted-foreground">{metrics ? 'RUNTIME VALUE' : 'WAITING FOR RUNTIME'}</div></div>)}</div><div className="mt-12 grid gap-3 lg:grid-cols-[1.2fr_.8fr]"><div className="border hairline bg-card p-6"><div className="flex items-center gap-3"><BrainCircuit className="size-5 text-accent" /><SectionLabel>AI DECISION PIPELINE</SectionLabel></div><div className="mt-8 grid grid-cols-2 gap-2 sm:grid-cols-3">{['COLLECT','CLASSIFY','FORECAST','DETECT','PLAN','VERIFY'].map((x,i)=><div key={x} className="border hairline p-4"><span className="mono text-[10px] text-muted-foreground">0{i+1}</span><div className="mt-5 text-xs">{x}</div></div>)}</div></div><div className="border hairline bg-card p-6"><div className="flex items-center gap-3"><AlertTriangle className="size-4 text-muted-foreground" /><SectionLabel>EVENT TIMELINE</SectionLabel></div><div className="mt-8 space-y-4 mono text-[10px] text-muted-foreground"><div className="flex gap-3"><Circle className="mt-0.5 size-2 fill-current" /> {metrics ? 'Telemetry frame received' : 'Waiting for event stream'}</div><div className="flex gap-3"><Circle className="mt-0.5 size-2" /> Root-cause analysis unavailable</div><div className="flex gap-3"><Circle className="mt-0.5 size-2" /> Optimization proof unavailable</div></div></div></div><div className="mt-10"><ArrowLink href="/documentation">Read runtime documentation</ArrowLink></div></div></Shell> }
